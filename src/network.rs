use crate::layer::Layer;



struct Network {
    layers: Vec<Layer>,
    net_total_layers: u8,
    net_inputs: Vec<i8>,
    net_outputs: Vec<i8>,
    net_layers:Vec<u8>,
    net_learning_rate: i8
}

impl Network {

}